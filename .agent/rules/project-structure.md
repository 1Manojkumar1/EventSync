---
trigger: always_on
---

This is a Next.js project.

Rules:
- App Router is used
- page.tsx files must contain UI only
- Business logic lives in /lib
- Hooks live in /hooks
- Firebase logic lives in /lib/firebase
- Components must be small and reusable
- Do not refactor unrelated files
